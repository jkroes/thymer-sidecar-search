# --- linked into the shared cache, NOT committed (run ./setup.sh to materialize) ---
/sdk
/bin/thymercli
/examples

node_modules/
.DS_Store
*.log
# dist/plugin.js is committed on purpose — it's the pasteable bundle.

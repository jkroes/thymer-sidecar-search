{
  "ver": 1,
  "name": "Sidecar Search",
  "icon": "search",
  "description": "Cmd-K replacement popup with native-palette parity: fuzzy-ranked search over collections (incl. hidden), views, pages; inline New/Open/Settings/Create rows; per-collection sub-palette; date jump rows; Shift+Enter opens in other panel. Native palette stays on Cmd+Shift+P."
}
